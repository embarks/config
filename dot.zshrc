# rust
export PATH="$HOME/.cargo/bin:$PATH"

fpath=(~/.zsh $fpath)
PS1="%n %~ $ "


alias la='ls -la'
alias c.='cd ..'
alias ghist="history | grep"
alias rustdoc="rustup docs --book"
alias yar="yarn"
alias run="npm run"
alias szsh="cat ~/.zshrc"% 
